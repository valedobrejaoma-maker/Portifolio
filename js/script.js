const menu=document.getElementById('menu'),app=document.getElementById('app');secoes.forEach(s=>{menu.innerHTML+=`<a href="#${s.id}">${s.titulo}</a>`;let sec=document.createElement('section');sec.id=s.id;sec.style.background=s.cor;sec.innerHTML=`<h2>${s.titulo}</h2><div class='cards'></div>`;let cards=sec.querySelector('.cards');s.produtos.filter(p=>p.visivel).forEach(p=>cards.innerHTML+=`<div class='card'><h3>${p.nome}</h3><p>${p.preco}</p></div>`);app.append(sec);});const obs=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.animate([{opacity:0,transform:'translateY(30px)'},{opacity:1,transform:'translateY(0)'}],{duration:500});}}));document.querySelectorAll('section').forEach(x=>obs.observe(x));